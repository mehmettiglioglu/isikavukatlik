// Tailwind v4: Konfigürasyon CSS-first'e taşındı (app/globals.css).
// Bu dosya v4'te kullanılmaz, @theme ve @plugin direktifleri globals.css içindedir.
import type { Config } from "tailwindcss";

const config: Config = {};
export default config;
